import MdxExample from "../components/MdxExample.astro";
import { withBase } from "../lib/utils/paths";

# MDX表示確認

Markdown本文として見出しや段落を書けることを確認するための最小ページです。

<MdxExample label="Astro ComponentをMDX内から表示しています。" />

<a href={withBase("/")}>トップページへ戻る</a>
