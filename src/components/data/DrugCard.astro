---
import {
  formatDisplayText,
  formatDisplayValue,
  type DisplayValue,
} from "../../lib/utils/display-value";

interface Props {
  anchorId?: string;
  name: string;
  credit?: DisplayValue;
  timing?: DisplayValue;
  setQuantity?: DisplayValue;
  badTripIntensity?: DisplayValue;
  effect?: string | null;
}

const { anchorId, name, credit, timing, setQuantity, badTripIntensity, effect } = Astro.props;
---

<article id={anchorId} class="data-card drug-card" data-drug-card>
  <div class="data-card-content">
    <header class="data-card-header">
      <span class="data-card-name">{name}</span>
    </header>

    <p class="data-card-meta"><span class="data-card-credit">信用：{formatDisplayValue(credit)}</span></p>
    <p class="data-card-meta data-card-meta-secondary">
      <span>使用タイミング：{formatDisplayValue(timing)}</span>
    </p>

    <dl class="data-card-details">
      <div class="data-card-detail">
        <dt>1セット数量</dt>
        <dd>{formatDisplayValue(setQuantity)}</dd>
      </div>
      <div class="data-card-detail drug-card-detail-bad-trip">
        <dt>BT強度</dt>
        <dd>{formatDisplayValue(badTripIntensity)}</dd>
      </div>
    </dl>

    <div class="data-card-effect">
      <p class="data-card-rule">{formatDisplayText(effect)}</p>
    </div>
  </div>
</article>

<style>
  .drug-card-detail-bad-trip dt {
    white-space: nowrap;
  }
</style>
