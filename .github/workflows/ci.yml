name: Quality CI

on:
  push:
    branches-ignore:
      - main
    paths-ignore:
      - ".codex/**/*.toml"
permissions:
  contents: read

jobs:
  root-quality:
    uses: ./.github/workflows/quality.yml

  changes:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      frontend: ${{ steps.filter.outputs.frontend }}
      shared: ${{ steps.filter.outputs.shared }}
      backend: ${{ steps.filter.outputs.backend }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Detect changed paths
        id: filter
        uses: dorny/paths-filter@v4
        with:
          filters: |
            frontend:
              - 'frontend/**'
              - 'package.json'
              - 'package-lock.json'
              - 'biome.json'
              - 'tsconfig.json'
              - '.github/workflows/**'
            shared:
              - 'packages/shared/**'
              - 'package.json'
              - 'package-lock.json'
              - 'biome.json'
              - 'tsconfig.json'
              - '.github/workflows/**'
            backend:
              - 'backend/**'
              - 'package.json'
              - 'package-lock.json'
              - 'biome.json'
              - 'tsconfig.json'
              - '.github/workflows/**'

  frontend-test:
    needs:
      - root-quality
      - changes
    if: ${{ needs.changes.outputs.frontend == 'true' }}
    uses: ./.github/workflows/workspace-test.yml
    with:
      workspace: '@neon-underrealm/frontend'
      script: test:coverage

  shared-test:
    needs:
      - root-quality
      - changes
    if: ${{ needs.changes.outputs.shared == 'true' }}
    uses: ./.github/workflows/workspace-test.yml
    with:
      workspace: '@neon-underrealm/shared'
      script: test

  backend-test:
    needs:
      - root-quality
      - changes
    if: ${{ needs.changes.outputs.backend == 'true' }}
    uses: ./.github/workflows/workspace-test.yml
    with:
      workspace: backend
      script: test

  backend-integration:
    needs:
      - root-quality
      - changes
    if: ${{ needs.changes.outputs.backend == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run local Worker integration test
        run: npm --workspace=@neon-underrealm/backend run test:integration:local
