# 40-2-404-page

## 目的

存在しないURLへアクセスした利用者に、画像、ページが存在しない旨、トップページへ戻るリンクを順に示すシンプルな404ページを提供する。

## 背景

`docs/plan.md` の `40-2-404-page` で、静的ホスティング上で機能する `/404` を追加することが予定されている。

対応する `.raw/contents/404.md` はローカルに存在しない。そのため、ページ本文と可視構成はユーザーの最新指示を正本とする。

- ユーザー提供の `public/images/404.webp` を先頭に表示する。
- その下に「お探しのページは存在しません」を表示する。
- さらにその下に「トップに戻る」のリンクを表示する。
- H1は文書構造のために `404 Not Found` として置くが、視覚的には非表示にする。

関連する要件と参照は以下とする。

- `docs/requirements/pages.md` の FR-11
- `docs/requirements/layout-navigation.md` の `/404` では PageToc を表示しない要件
- `docs/design/page-toc/design-desktop-hidden-404.png` と `notes.md` の、404ではPageTocの右レールを表示しない参照
- `docs/plan.md` の `40-2-404-page`

`docs/requirements/pages.md` はサイトメニューまたは検索への導線を求めるが、既存の `AppContainer` / `NoTocPageLayout` が共有SiteMenuを提供する。このissueでは、ページ本文へ追加のサイトメニューまたは検索UIを置かず、トップページへ戻るリンクだけを追加する。

## 対象範囲

- `src/pages/404.astro` を追加し、Astroの404ルートとして機能させる。
- 既存の `AppContainer` を利用し、PageTocを表示しない。
- ユーザー提供済みの `public/images/404.webp` を、既存のbase path対応方式で404ページの画像として参照する。画像バイナリはこのtaskで加工または置換しない。
- 画像の代替テキストは `404 Not Found` とする。
- 表示順を「画像」「お探しのページは存在しません」「トップに戻る」にする。
- H1を `404 Not Found` としてアクセシブルな文書構造に保持しつつ、視覚的には非表示にする。
- トップへの内部リンクでは既存のサブパス対応方式を用いる。
- 404ページをVisual Review対象に追加する。存在しないサブパスへアクセスしてdesktop・mobileのスクリーンショットだけを取得するVisual Testを追加する。

## 初期スコープ外

- 404専用の検索UI、サイトメニュー、パンくず、追加ナビゲーションを実装しない。
- PageTocおよびモバイルPageTocを表示しない。
- 実装前の404専用designドラフトを新規作成しない。
- Visual Reviewのactual screenshotを `docs/design/` へ直接コピーまたはcanonicalizeしない。
- ユーザー提供済み `public/images/404.webp` を加工、差し替え、再生成しない。
- 共通Layout、Header、Footer、SiteMenu、検索機能、PageTocの仕様を変更しない。
- 動的な404処理、SSR、CMS、分析、キャラクターシート等の初期スコープ外機能を追加しない。

## 完了条件

- [x] `/404` を生成する `src/pages/404.astro` がある。
- [x] 404ページで、ユーザー提供画像、「お探しのページは存在しません」、「トップに戻る」の順に表示される。
- [x] 画像の `alt` と、視覚的に隠すH1の文言がともに `404 Not Found` である。
- [x] H1は存在し、視覚的には非表示である。
- [x] 画像がGitHub Pagesのサブパス配下でも表示される。
- [x] 「トップに戻る」がGitHub Pagesのサブパス配下でもトップページへ遷移する。
- [x] PageTocとモバイルPageTocが404ページに表示されない。
- [x] 新規のサイトメニューまたは検索UIをページ本文へ追加していない。
- [x] 404専用の初期designドラフトを作成していない。ユーザー指定により、実装前の `design-image-generation` は実行不要と記録されている。
- [x] 存在しないサブパスへアクセスしてdesktop・mobileのスクリーンショットだけを取得するVisual Testがある。
- [x] 実装後にVisual Reviewでactual screenshotを取得・記録し、`## ビジュアルレビュー N` を現在のissueへ追加している。
- [x] actual screenshotが404のdesign正本候補になる場合は、Visual Reviewで候補として記録して停止し、ユーザー承認後に `design-image-generation` のdesign fix modeへ引き継ぐ。
- [x] `npm run check` が通る。
- [x] `npm run build` が通る。
- [x] `npm run build` 後に `dist/404.html` が生成される。

## チェックポイント

- [x] 既存ルートが壊れていない。
- [x] GitHub Pagesのサブパス公開に影響しない。
- [x] 不要な依存関係を追加していない。
- [x] 初期スコープ外の機能を実装していない。
- [x] 関連する `docs/TODO.md` 項目と矛盾していない。確認した範囲では関連TODOはない。
- [x] `docs/design/page-toc/` の404におけるPageToc非表示の参照と矛盾していない。
- [x] preview環境で、サブパス配下の存在しないURLが404ステータスと今回の本文を返すことを確認している。
- [x] Visual Testは、存在しないサブパスへのアクセスとスクリーンショット取得だけを行い、追加の画面アサーションを増やしていない。
- [x] ユーザーの未コミット変更である `public/images/404.webp` および `public/images/data/items/` を破壊していない。

## 想定変更ファイル

- `src/pages/404.astro`
- `tests/visual/config.ts`
- `tests/visual/not-found.spec.ts`
- `docs/design/404/notes.md`
- `docs/design/404/design-desktop.png`
- `docs/design/404/design-mobile.png`
- `docs/issue/40-2-404-page.md`

利用対象（ユーザー提供済み・この準備では未変更）:

- `public/images/404.webp`

## レビュー観点

- 本文の可視構成が、画像、案内文、トップリンクだけのシンプルな順序になっているか。
- H1を見せずに文書構造とアクセシビリティを損ねない方針が適切か。
- 画像の `alt` と隠すH1をともに `404 Not Found` とする文言が適切か。
- 共有レイアウトのSiteMenuを維持しつつ、404本文に余計なメニューまたは検索UIを増やさない範囲が適切か。
- `/404` でPageTocの右レールおよびモバイルPageTocが出ないことを確認できる条件になっているか。
- 存在しないサブパスへのアクセスとスクリーンショット取得だけを行うVisual Testの範囲が適切か。
- 実装前のdesignドラフトを省略し、Visual Reviewではactualを記録するだけに留め、design正本化はユーザー承認後のdesign fix modeへ分離する方針が適切か。
- 静的ホスティング成果物の `dist/404.html` と、未知URLへの404応答を確認する完了条件が十分か。

## 備考

- ユーザーは「単純な画面なのでdesignドラフトスキップ」と明示した。そのため、`docs/plan.md` の「designを生成する」は実装前提から外す。実装後のVisual Reviewではactual screenshotを記録する。design正本化が必要になった場合は、ユーザー承認後に `design-image-generation` のdesign fix modeで扱う。`docs/plan.md` のチェックボックスは変更しない。
- 既存の `AppContainer` は `showPageToc` の既定値が `false` であり、`NoTocPageLayout` を選択する。実装時もこの経路を利用してPageToc非表示を保つ。
- この準備時点で `.raw/contents/404.md` は未存在である。後から作成された場合は、そのMarkdown本文とHTMLコメントをページ本文・可視構成の上位正本として再確認する。

## ビジュアルレビュー 1

### デザイン参照

- design target: `docs/design/page-toc/`（Visual Review）および `docs/design/404/`（承認後にcanonicalize）
- reference desktop: `design-desktop-hidden-404.png`
- reference mobile: なし。ユーザー指定のシンプルな可視構成を正本とする。
- notes: 404ではPageTocおよび空の右レールを表示しないことを確認する。404専用の初期designドラフトはユーザー指定により省略した。

### 成果物

- actual desktop: `test-results/visual/404-desktop.png`
- actual mobile: `test-results/visual/404-mobile.png`
- report: `test-results/visual/capture-manifest.json`

### レビュー結果

| 領域                  | 判定 | 差分 | 対応                                                      |
| --------------------- | ---- | ---- | --------------------------------------------------------- |
| レイアウト            | OK   | なし | 画像、案内文、トップリンクを中央に配置した。              |
| 余白                  | OK   | なし | 既存のspacing tokenだけを使用した。                       |
| タイポグラフィ        | OK   | なし | H1は視覚的に隠し、案内文とリンクを表示した。              |
| 色                    | OK   | なし | 既存のページ色とリンク色を使用した。                      |
| 配置・整列            | OK   | なし | desktop・mobileともに画像とテキストの中央揃えを確認した。 |
| レスポンシブ          | OK   | なし | desktop 1440pxとmobile 390pxで確認した。                  |
| overflow / scroll     | OK   | なし | 横overflowは確認されなかった。                            |
| 既存デザインとの整合  | OK   | なし | PageToc非表示の404参照と整合する。                        |
| 既存Componentとの整合 | OK   | なし | `AppContainer` と `InternalLink` を使用した。             |
| accessibility basics  | OK   | なし | H1と画像altはともに `404 Not Found` である。              |

### 自己修正した項目

- [x] `showPageToc={false}` を明示し、PageTocを表示しない。

### 人間判断が必要な差分

- ユーザーが2026-07-23に、404のactual screenshotをdesign正本化することを明示承認した。

### design-image-generation への引き継ぎ候補

- [x] ユーザー承認に基づき、`docs/design/404/` をdesign fix modeで正本化した。

### 対応完了チェックリスト

- [x] desktop screenshot を取得した
- [x] mobile screenshot を取得した
- [x] reference と actual を比較した
- [x] 明らかな visual mismatch はなかった
- [x] ユーザー承認後にdesign fix modeでdesign正本を更新した
- [x] `npm run check` が通る
- [x] `npm run build` が通る
