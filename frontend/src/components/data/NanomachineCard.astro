---
import {
  formatDisplayText,
  formatDisplayValue,
  type DisplayValue,
} from "../../lib/utils/display-value";

interface Props {
  anchorId?: string;
  name: string;
  credit?: DisplayValue;
  activationMentalCost?: DisplayValue;
  implantPoints?: DisplayValue;
  effect?: string | null;
}

const { anchorId, name, credit, activationMentalCost, implantPoints, effect } = Astro.props;
---

<article id={anchorId} class="data-card nanomachine-card" data-nanomachine-card>
  <div class="data-card-content">
    <header class="data-card-header">
      <span class="data-card-name">{name}</span>
    </header>

    <p class="data-card-meta"><span class="data-card-credit">信用：{formatDisplayValue(credit)}</span></p>

    <dl class="data-card-details">
      <div class="data-card-detail">
        <dt>発動精神力</dt>
        <dd>{formatDisplayValue(activationMentalCost)}</dd>
      </div>
      <div class="data-card-detail">
        <dt>埋め込み点数</dt>
        <dd>{formatDisplayValue(implantPoints)}</dd>
      </div>
    </dl>

    <div class="data-card-effect">
      <p class="data-card-rule">{formatDisplayText(effect)}</p>
    </div>
  </div>
</article>
